import { Routes } from '@angular/router';
import { CouponListComponent } from './components/coupon-list/coupon-list.component';

export const routes: Routes = [{ path: '', component: CouponListComponent }];
